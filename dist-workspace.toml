[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.33.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-unknown-linux-musl", "x86_64-pc-windows-msvc", "x86_64-unknown-linux-musl"]
# Path that installers should place binaries in
install-path = ["$LOCALAPPDATA/Programs/agent-statusline", "~/.local/bin"]
# Where to host releases
hosting = "github"
# Archive format for each platform (use zip uniformly for zero-dependency extraction)
unix-archive = ".zip"
windows-archive = ".zip"
# Whether to install an updater program
install-updater = false
